{
  "name": "skyground_test",
  "packageManager": "yarn@4.3.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "yarn workspaces foreach --all run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
